ROOT = File.expand_path('..', __FILE__)

namespace :test do
  task :features do
  end

  task :unit do
  end
end

task :example do
  require ROOT + '/examples/bowling.test.rb'
end

task :default => 'test:features'
